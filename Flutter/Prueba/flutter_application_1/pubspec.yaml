name: ble_testapp
description: flutter ble testapp
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  permission_handler: ^10.2.0
  flutter_reactive_ble: ^5.3.1
  location_permissions: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true